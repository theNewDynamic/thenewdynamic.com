name: "regular-pages-fr"
label: "Regular Pages (FR)"
label_singular: "Regular Page"
description: The regular pages.
folder: "content/fr"
filter: { field: "exclude_collection" }
create: true
sortableFields: ['title']
preview_path: 'fr/{{slug}}'
fields:
  - import field title
  - import field description
  - import field body
  - import field blocks
  - import field seo
