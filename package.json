{
  "name": "thenewdynamic.com",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {  
    "hugo": "hugo serve --gc --disableFastRender ",
    "build": "NODE_ENV=production hugo --gc --minify",
    "deploy": "yarn build",
    "start": "yarn hugo"
  },
  "dependencies": {
    "autoprefixer": "^9.7.3",
    "postcss-cli": "^7.1.0",
    "tailwindcss": "^1.2.0-canary.5"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0"
  }
}
